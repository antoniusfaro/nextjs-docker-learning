# services:
#   nextjs-app:
#     build:
#       context: .
#       dockerfile: Dockerfile

#     ports:
#       - "3000:3000"

#     restart: unless-stopped
services:
  nextjs-app:
    image: ghcr.io/antoniusfaro/nextjs-docker-learning:latest

    ports:
      - "127.0.0.1:3000:3000"

    restart: unless-stopped
